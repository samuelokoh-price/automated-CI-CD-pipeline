global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100']

  - job_name: 'docker_engine'
    static_configs:
      - targets: ['localhost:9323'] # ⚡ OPTIMIZED: Routes via host loopback safely
