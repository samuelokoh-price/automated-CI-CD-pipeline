apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090 # ⚡ FIXED: Matches host network mode deployment
    isDefault: true
    uid: "PROMETHEUS_PROD_ENGINE"
    editable: true
    version: 2
    jsonData:
      httpMethod: POST
